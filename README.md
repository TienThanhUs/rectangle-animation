# rectangle-animation
This project is a simple 2D graphics program written in C using the raylib library. It creates a window and renders a blue rectangle that continuously moves and bounces around the screen edges.
